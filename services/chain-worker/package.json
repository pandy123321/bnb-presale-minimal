{
  "name": "@pangu2/chain-worker",
  "version": "1.0.0",
  "private": true,
  "description": "PANGU2 Chain Worker — scans blockchain events, detects reorgs, writes raw events to PostgreSQL",
  "main": "src/index.ts",
  "scripts": {
    "dev": "tsx src/index.ts",
    "start": "tsx src/index.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "verify-abi": "tsx scripts/verify-abi-artifacts.ts"
  },
  "dependencies": {
    "viem": "^2.22",
    "pg": "^8.13",
    "node-cron": "^3.0"
  },
  "devDependencies": {
    "@types/node": "^22.0",
    "@types/pg": "^8.11",
    "@types/node-cron": "^3.0",
    "tsx": "^4.19",
    "typescript": "^5.7",
    "vitest": "^2.1",
    "dotenv": "^16.4"
  }
}
