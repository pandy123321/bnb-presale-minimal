# BSC Testnet Fork Configuration
# This file is the single authoritative source for all fork test parameters.
# The CI workflow reads this file directly. No GitHub Secret or Variable overrides any value here.
#
# FORK_RPC: Public BSC Testnet RPC endpoint (no API key required).
#   External fork PRs and Dependabot PRs use the same public endpoint.
# FORK_BLOCK: Fixed, approved block number for deterministic fork testing.
# FORK_BLOCK_HASH: Expected hash at that block. CI will fail if the RPC returns a different hash.

FORK_RPC=https://bsc-testnet-rpc.publicnode.com
FORK_BLOCK=44780000
FORK_BLOCK_HASH=
