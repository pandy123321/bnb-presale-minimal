# BSC Testnet Fork Block - Canonical Source
# This file is the single authoritative source for the fork block number used in CI fork tests.
# The CI workflow reads this file directly; no GitHub Variable or Secret contains a fork block number.
# 
# Update this file and commit to change the fork point. Fork tests in CI will automatically use the new value.
# The RPC URL is configured separately as a GitHub Actions secret (BSC_TESTNET_RPC_URL).

FORK_BLOCK=44780000
